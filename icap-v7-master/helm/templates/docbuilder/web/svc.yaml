apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-docbuilder-web
spec:
  ports:
  - port: 5000
    targetPort: 5000
  selector:
    application: {{ .Release.Name }}-docbuilder-web
  type: ClusterIP
