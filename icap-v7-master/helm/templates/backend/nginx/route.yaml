apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ .Release.Name }}-backend-nginx
  annotations:
    haproxy.router.openshift.io/timeout: {{ .Values.backend.nginx.env.PROXY_TIMEOUT }}s
spec:
  host: {{ .Values.backend.nginx.route.host }}
  port:
    targetPort: 8080
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
  to:
    name: {{ .Release.Name }}-backend-nginx