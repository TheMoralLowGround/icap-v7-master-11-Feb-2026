apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-backend-nginx
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    application: {{ .Release.Name }}-backend-nginx
  type: ClusterIP