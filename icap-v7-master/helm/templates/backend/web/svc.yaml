apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-backend-web
spec:
  ports:
  - port: 8000
    targetPort: 8000
  selector:
    application: {{ .Release.Name }}-backend-web
  type: ClusterIP