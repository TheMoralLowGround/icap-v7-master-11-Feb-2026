apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-input-channel-web
spec:
  ports:
  - port: 8000
    targetPort: 8000
  selector:
    application: {{ .Release.Name }}-input-channel-web
  type: ClusterIP