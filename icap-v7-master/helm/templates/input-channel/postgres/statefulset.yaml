apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Release.Name }}-input-channel-postgres
  labels:
    application: {{ .Release.Name }}-input-channel-postgres
spec:
  replicas: {{ .Values.inputChannel.postgres.replicas }}
  serviceName: {{ .Release.Name }}-input-channel-postgres
  selector:
    matchLabels:
      statefulset: {{ .Release.Name }}-input-channel-postgres
  template:
    metadata:
      labels:
        application: {{ .Release.Name }}-input-channel-postgres
        statefulset: {{ .Release.Name }}-input-channel-postgres
    spec:
      volumes:
        - name: pgsqldata
          persistentVolumeClaim:
            claimName: {{ .Values.common.pvc.inputChannelPostgresData }}
        - name: db-backup
          persistentVolumeClaim:
            claimName: {{ .Values.common.pvc.dbBackup }}
      containers:
        - name: postgres
          image: {{ .Values.inputChannel.postgres.image }}
          resources:
            {{- toYaml .Values.inputChannel.postgres.resources | nindent 12 }}
          env:
            - name: POSTGRESQL_DATABASE
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name}}-input-channel-postgres-db-initial-creds
                  key: DB_NAME
            - name: POSTGRESQL_USER
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name}}-input-channel-postgres-db-initial-creds
                  key: DB_USERNAME
            - name: POSTGRESQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name}}-input-channel-postgres-db-initial-creds
                  key: DB_PASSWORD
            - name: POSTGRESQL_VERSION
              value: {{ .Values.inputChannel.postgres.env.postgresqlVersion | quote }}
          ports:
            - containerPort: 5432
              protocol: TCP
          volumeMounts:
            - name: pgsqldata
              mountPath: /var/lib/pgsql/data
            - name: db-backup
              mountPath: /dbdump
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - pg_isready -U $POSTGRESQL_USER -d $POSTGRESQL_DATABASE -h localhost
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5
          readinessProbe:            
            exec:
              command:
                - /bin/sh
                - -c
                - pg_isready -U $POSTGRESQL_USER -d $POSTGRESQL_DATABASE -h localhost
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5