apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-ocr-engine-web
spec:
  ports:
  - port: 5000
    targetPort: 5000
  selector:
    application: {{ .Release.Name }}-ocr-engine-web
  type: ClusterIP
