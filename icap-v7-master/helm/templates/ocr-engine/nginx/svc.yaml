apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-ocr-engine-nginx
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    application: {{ .Release.Name }}-ocr-engine-nginx
  type: ClusterIP
