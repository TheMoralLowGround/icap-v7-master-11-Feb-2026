apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-utility-nginx
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    application: {{ .Release.Name }}-utility-nginx
  type: ClusterIP
