apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-classifier-web
spec:
  ports:
  - port: 8000
    targetPort: 8000
  selector:
    application: {{ .Release.Name }}-classifier-web
  type: ClusterIP