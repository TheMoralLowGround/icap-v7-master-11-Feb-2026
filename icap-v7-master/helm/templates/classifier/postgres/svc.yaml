apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-classifier-postgres
spec:
  clusterIP: None  # Headless service
  selector:
    application: {{ .Release.Name }}-classifier-postgres
  ports:
  - port: 5432
    targetPort: 5432